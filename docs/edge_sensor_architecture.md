# Edge Sensor – SoC architecture

Brief high-level view of the SoC: CPU (master), interconnect (xbar), and slaves
(peripherals and memory). The data-path bus is **TL-UL** (from `soc.bus`).
Protocol variants (e.g. AXI4 vs AXI4-Lite) are specified in `soc.bus` and in each
IP's metadata; bridges (e.g. tlul-axi-bridge: AXI4-Lite slave, TL-UL host) declare
both sides in their interfaces. Explicit pin connections are in the connectivity schema.

## Block diagram (ASCII)

```
                                          +------------+
                                          |   u_ibex   |
                                          +------------+
                                                 |
  ==============================================================================================  TL-UL (xbar_main)
         |               |               |               |               |               |      
  +------------+  +------------+  +------------+  +------------+  +------------+  +------------+
  |   u_uart   |  | u_spi_host |  |   u_plic   |  |   u_fft    |  |   u_rom    |  |   u_ram    |
  +------------+  +------------+  +------------+  +------------+  +------------+  +------------+
     (periph)        (periph)        (periph)        (periph)         (mem)           (mem)     
```

- **Master**: `u_ibex` drives the interconnect.
- **Interconnect**: `xbar_main` routes TL-UL transactions to slaves.
- **Bus**: `soc.bus` = TL-UL (IPs connect via the protocol they declare in metadata).
- **Peripherals**: u_uart, u_spi_host, u_plic, u_fft
- **Memory**: u_rom, u_ram

## Explicit connections (from connectivity schema)

- `u_ibex.core_sleep_o` -> `soc.core_sleep`
## IP interfaces (from vyges-metadata.json)

- **u_fft** (fast-fourier-transform-ip):  ?;  ?; slave APB: APB slave control interface. Registers: FFT_CTRL (start/reset), FFT_STATUS (busy/done/error), FFT_LENGTH, twiddle write window at offset 0x800.; output level
- **u_ibex** (opentitan-rv-core-ibex): input custom: Defines the memory region matching configuration.; input custom: Specifies the remap address for the memory region.; output custom: Indicates whether the core is in a sleeping state.
- **u_plic** (vyges-rv-plic-lite): input generic: Clock input signal; input active-low: Active-low reset signal; slave TL-UL: TL-UL host-to-device interface; slave TL-UL: TL-UL device-to-host interface; input level-sensitive: Interrupt source inputs (active-high, level-sensitive); output level-sensitive: Interrupt output to hart
- **u_spi_host** (vyges-spi-host-lite): input generic: System clock input.; input generic: Active-low system reset input.; slave TL-UL: TL-UL host-to-device interface input.; master TL-UL: TL-UL device-to-host interface output.; output SPI: SPI clock output.; output SPI: SPI chip-select output (active low).; output SPI: SPI master-out slave-in data output.; input SPI: SPI master-in slave-out data input.; output generic: Interrupt output for TX FIFO empty condition.; output generic: Interrupt output for RX FIFO full condition.; output generic: Interrupt output for SPI idle condition.
- **u_uart** (opentitan-uart): slave TL-UL: Incoming TL-UL bus interface for register access.; master TL-UL: Outgoing TL-UL bus interface for register responses.; input generic: Clock input for synchronous operation.; input generic: Active-low reset input.; output generic: Integrity error interrupt output.

## Related artifacts

- Address map: `pkg/edge_sensor_pkg.sv`
- Interconnect RTL: `rtl/xbar_main.sv`
- Block diagram: `docs/edge_sensor_block_diagram.svg`

---

*© 2026 Vyges. All Rights Reserved. Generated by Vyges SoC Generator.*
