<!-- @generated by vyges-soc-generator -->
# edge_sensor — fpga PCBA

**Target:** FPGA daughterboard (arty_a7_100t)
**KiCad version:** kicad8
**Generated:** 2026-04-12T05:24:58Z

## Host board

`arty_a7_100t` — daughterboard plugs into the host board's Pmod / FMC / shield connector. The host board itself is upstream and not generated here.

## Generated files

| File | Contents |
| ---- | -------- |
| `edge_sensor_fpga.kicad_pro` | KiCad project file |
| `edge_sensor_fpga.kicad_sch` | Schematic skeleton (one symbol per component, one wire per net) |
| `edge_sensor_fpga.net` | **Authoritative** KiCad netlist — this is the source of truth for connectivity |
| `bom/edge_sensor_fpga_bom.csv` | Bill of materials |
| `inspection/components.txt` | Human-readable components listing (no KiCad required) |
| `inspection/nets.txt` | Human-readable nets listing + `soc.caravel.gpio[]` cross-reference |
| `inspection/power.txt` | Human-readable power tree |
| `reports/` | Populated by `kicad-cli` on the Ubuntu box — ERC, DRC (Phase 3) |
| `gerbers/` | Populated by `kicad-cli` on the Ubuntu box — gerbers, drill files, STEP, SVG (Phase 3) |

## Summary

- **Components:** 4
- **Nets:** 6
- **Power rails:** 1
- **Layers:** 2
- **Outline:** 30 × 20 mm- **Surface finish:** HASL

## Template

No template fork — this board is generated from scratch (small, simple layouts only).

## Next steps

Phase 2 emitted the KiCad project, schematic skeleton, netlist, and BOM. Phase 3
(deferred) runs on the Ubuntu box after rsync:

```bash
kicad-cli sch erc     edge_sensor_fpga.kicad_sch -o reports/erc.json
kicad-cli pcb drc     edge_sensor_fpga.kicad_pcb -o reports/drc.json
kicad-cli sch export bom     edge_sensor_fpga.kicad_sch -o bom/edge_sensor_fpga_bom.csv
kicad-cli pcb export gerbers edge_sensor_fpga.kicad_pcb -o gerbers/
kicad-cli pcb export drill   edge_sensor_fpga.kicad_pcb -o gerbers/
kicad-cli pcb export step    edge_sensor_fpga.kicad_pcb -o edge_sensor_fpga.step
kicad-cli pcb export svg     edge_sensor_fpga.kicad_pcb -o inspection/board.svg \
    --layers "F.Cu,B.Cu,F.SilkS,Edge.Cuts"
```
